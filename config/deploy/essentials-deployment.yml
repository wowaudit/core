apiVersion: apps/v1
kind: Deployment
metadata:
  name: essentials-deployment
  labels:
    app: essentials
spec:
  replicas: 16
  selector:
    matchLabels:
      app: essentials
  template:
    metadata:
      labels:
        app: essentials
    spec:
      containers:
      - name: essentials
        image: shedi/wowaudit:latest
        command: ["/srv/bin/refresh"]
        args: ["essentials"]

        resources:
          requests:
            cpu: 250m
            memory: 200Mi

          limits:
            cpu: 250m
            memory: 400Mi

      imagePullSecrets:
        - name: regcred
